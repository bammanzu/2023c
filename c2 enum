#include <stdio.h>

// 열거형 정의
enum Days {
    Sunday,
    Monday,
    Tuesday,
    Wednesday,
    Thursday,
    Friday,
    Saturday
};

int main() {
    // 열거형 변수 선언 및 초기화
    enum Days today = Wednesday;

    // 열거형 값 출력
    printf("오늘은 ");
    
    switch (today) {
        case Sunday:
            printf("일요일");
            break;
        case Monday:
            printf("월요일");
            break;
        case Tuesday:
            printf("화요일");
            break;
        case Wednesday:
            printf("수요일");
            break;
        case Thursday:
            printf("목요일");
            break;
        case Friday:
            printf("금요일");
            break;
        case Saturday:
            printf("토요일");
            break;
    }

    printf(" 입니다.\n");

    return 0;
}
/*C 언어에서 enum은 열거형(enumeration)을 정의하는 데 사용되며, 특정 상수 값들의 집합을 나타냅니다. 
열거형은 코드를 읽기 쉽게 만들고, 가독성을 향상시키며, 오류를 줄이는 데 도움이 됩니다.*/

/*이 예제에서는 Days라는 열거형을 정의하고, 그 안에 일주일의 요일을 나타내는 상수 값을 정의하였습니다.
main 함수에서는 today라는 열거형 변수를 선언하고 Wednesday로 초기화한 후, switch 문을 사용하여
today의 값에 따라 오늘 요일을 출력합니다.*/
